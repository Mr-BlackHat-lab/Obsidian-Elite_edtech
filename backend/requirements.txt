fastapi>=0.111.0
uvicorn[standard]>=0.30.0
motor>=3.5.1
redis>=5.0.0
celery>=5.4.0
youtube-transcript-api
openai-whisper
# NOTE: ffmpeg binary is required by openai-whisper for audio decoding.
# It is installed via apt-get in the Dockerfile (apt-get install -y ffmpeg).
# Do NOT add ffmpeg-python here — that is a different Python wrapper package.
spacy
sentence-transformers
faiss-cpu
python-dotenv>=1.0.1
pydantic>=2.7.0
python-multipart
